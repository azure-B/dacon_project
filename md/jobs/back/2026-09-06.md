# back 작업 로그 — 2026-09-06

## — OpenAI API Key를 config/config.txt에서 로드하도록 연동
- 변경 파일: `back/config/aiConfig.js`, `back/.env.example`, `.gitignore`, `config/config.example.txt`, `md/folder.md`
- 동작: `OPENAI_API_KEY` / `AI_API_KEY`가 없으면 `config/config.txt` 첫 줄을 OpenAI 키로 사용
- 배포: Render 등에서는 `OPENAI_API_KEY` 환경변수 권장 (`config.txt`는 gitignore)

## — 가계부 월 필터 버그 수정 + 시뮬레이션 API + 사이드바
- 변경 파일: `back/models/accountBook.model.js`, `back/models/simulation.model.js`, `back/controller/simulation.controller.js`, `back/routes/simulation.routes.js`, `front/src/pages/Simulation/Simulation.jsx`, `front/src/pages/AiReport/AiReport.jsx`, `front/src/pages/AiFeedback/AiFeedback.jsx`, `front/src/services/api.js`, `md/API/README.md`
- 버그: 9월 등 31일 없는 달 필터가 `YYYY-MM-31`로 조회되어 500
- API: `POST /api/simulation`
- UI: AiReport/AiFeedback 사이드바 `min-h-[calc(100vh-4rem)]` + stretch

## — 시뮬레이션 시나리오 라벨·문구 구어체 정리
- 변경 파일: `back/models/simulation.model.js`
- `SCENARIO_LABELS`(현재 유지→지금처럼 쭉, 월 10만원 저축→한 달 10만원 모으기 등) 및 fallback insight/전략 텍스트를 프론트 카피 톤(구어체)에 맞춰 통일
- API 응답 스키마 변경 없음 (`POST /api/simulation` 응답 필드 동일)

## — 시뮬레이션 그래프 미변경·시나리오 고착 버그 수정
- 변경 파일: `back/models/simulation.model.js`, `back/controller/simulation.controller.js`
- 원인1: 프론트「그려보기」가 시나리오 없으면 무조건 `save`로 전송 → 카드가 계속 「한 달 10만원 모으기」처럼 보임
- 원인2: 자산·부채를 동일 절대 스케일로 그려 부채(수천만)에 자산 변화가 바닥에 붙어 그래프가 안 바뀌는 것처럼 보임 / 일시 상환(보너스 등) 미지원
- 수정:
  - `scenario` 기본값 `custom`, 프롬프트에서 금액·저축/상환 의도 추론(`inferFromPrompt`), AI에 `ls`/`lr`(일시 저축·상환 만원) 추가
  - 자산/부채 별도 스케일 + 부채 구간 줌으로 시나리오 차이가 눈에 보이게
  - hold 기준선을 월 3만원으로 고정해 save/repay 갭이 플러스로 보이게
- API: `POST /api/simulation` — `scenario`에 `custom` 추가 (기존 hold/save/repay/rate 유지)

## — 빚 정리 insight "80/45" 버그 + 가계부 6개월 시드
- 변경 파일: `back/models/debtAdjustment.model.js`, `back/models/spendingEvaluation.model.js`, `back/scripts/seed-and-evaluate.js`
- 원인: 시스템 프롬프트 `"insight":"<=80"`을 모델이 값 `80`으로 오해 → 화면엔 숫자만 표시
- 수정: 프롬프트를 `insight=한국어문장(최대80자)` 형태로 명확화, `pickText`로 숫자·`<=N`만 오면 fallback 문장 사용
- 시드: 최근 6개월(2026-04~09) 가계부 데이터 삽입(이미 있는 달은 skip), API_BASE 기본 5000
- 검증: 지출 평가 insight 정상 한국어 문장 반환 확인

## — 만약에 5년 소비편차 꺾인 그래프
- 변경 파일: `back/models/simulation.model.js`, `back/controller/simulation.controller.js`
- 기간: `1y`/`3y`/`5y`(기본 5년), 월별 소비 편차(계절·생활비 흔들림)로 자산/빚 path가 직선이 아니라 꺾이며 누적
- 응답 `chart.xLabels` 추가
- API: `POST /api/simulation`
